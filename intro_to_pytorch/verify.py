# Install pytorch using following commands, you can use either of them
# 1. conda install pytorch torchvision -c pytorch
# 2. pip install torch torchvision

from __future__ import print_function
import torch
x = torch.rand(5, 3)
print(x)
